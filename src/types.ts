export interface Countdown {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
}

export interface Exam {
  id: number;
  subject: string;
  date: string;
  time: string;
  session: string;
  fullTime: string;
}
